"""The leaf commands. One module per first-level noun.

Each module holds the classes for its group and nothing else -- the work lives in
the sibling modules (board, renderer, templates, images), so a command is only
ever "parse these flags, call that, choose an exit code".
"""
