# Backlog for .github/ISSUE_TEMPLATE/create_model.yaml
#
# Fill in the entries below, then:
#   python tools/pipeline/pipeline.py backlog file            # dry run -- validates every entry, creates nothing
#   python tools/pipeline/pipeline.py backlog file --apply    # create them; the numbers are written back here
#
# An entry that already has an `issue:` is skipped, so this is safe to re-run.
#
# Optional fields this template also accepts: reference-images

template: create_model

# Applied to every entry that does not set them itself.
defaults:
  # assignee: your-github-handle
  # priority: high

issues:
  # - name: example_asset
  #   description: This hamster is a cheerfull, cartoonish... Huge cheeks and wide eyes...
  #   dimentions: 1.8 meters tall x .3 meters deep, .5 meters wide
  #   filepath: assets/art/3d/{category}/{object_name}/sm_{object_name}.gltf
